# FastAPI & Core
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# CORS
python-multipart==0.0.6

# Database (SQLite built-in, PostgreSQL optional)
sqlalchemy==2.0.25
alembic==1.13.1

# PostgreSQL driver (optional - only needed for production PostgreSQL)
# Uncomment if using PostgreSQL instead of SQLite:
# psycopg2-binary==2.9.9

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2
email-validator>=2.0.0

# Utilities
python-dotenv==1.0.0
faker==22.5.1

# Agent Framework (with compatible versions)
# Note: Removing strict version pins to let pip resolve dependencies
langchain>=0.1.0
langchain-openai>=0.0.5
langgraph>=0.0.20
tiktoken>=0.5.0

# Vector Store
chromadb>=0.4.20

# Document Processing
pypdf>=3.0.0
python-docx>=0.8.11

# Task Queue (Optional - comment out if not using)
# celery==5.3.6
# redis==5.0.1

# Monitoring (Optional)
# prometheus-client==0.19.0

# Secrets Management (Optional)
# hvac==2.1.0

# Optional: For real LLM calls
openai>=1.10.0
